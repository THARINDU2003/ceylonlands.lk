services:
  - type: web
    name: ceylonterrece
    env: node
    plan: free
    branch: main
    rootDir: backend
    buildCommand: "npm install"
    startCommand: "node server.js"
    envVars:
      - key: JWT_SECRET
        generateValue: true
